миграции в Django?
что делает команда makemigartions?
что делает команда migrate?
зачем нужна команда createsuperuser?
как запустить Django проект?

Ответ:

1)миграция это синхронизация в джанго
2)makemigrations cоздает синхронизацию
3)migrate он уже синхронизирует
4)createsuperuser создает супер пользователя то есть админа
5)чтобы запустить джанго проект надо ввести в терминале
python manage.py runserver
